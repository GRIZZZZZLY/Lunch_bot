# 12 — `HomePage.tsx`: 433-строчное тело компонента, ~20 вызовов хуков, 6 багфиксов

- **Приоритет:** P2
- **Оценка:** 2 дня (тестов на барьер и на приоритет deep link ещё нет — их
  надо написать до правки)
- **Предпосылка:** задача 11, шаг 1 (без измерения покрытия рефакторинг фронта
  идёт вслепую)
- **Тип:** god-component
- **Область:** `frontend-new/src/features/home/HomePage.tsx`, `frontend-new/src/features/budget/BudgetPage.tsx`, `frontend-new/src/features/suggestions/SuggestionsPage.tsx`
- **Метод:** TDD — тесты на HomePage уже есть, опираться на них

> **Учтите при работе.** Перенесено из удалённого
> `TECHNICAL_AUDIT_2026-07-19.md` (см. [15.2](15-repo-hygiene.md)): в React
> Doctor **отключена ровно одна эвристика** — длина JSX-компонента. Семь
> отмеченных ею файлов тогда проверили вручную и признали приемлемыми:
> состояние и побочные эффекты уже вынесены в управляющие функции и дочерние
> блоки. Остальные правила Doctor продолжают блокировать CI. Значит «Doctor
> зелёный» **не является** доказательством, что god-компонент разрезан, — и
> наоборот, длина сама по себе не была признана дефектом.

## Метрики

| Файл | Строк | Багфиксов | Churn %ile | Health |
|---|---|---|---|---|
| `features/home/HomePage.tsx` | 506 (тело `HomePage()` — строки 73–506, ~433) | **6, метка `bug_magnet`** | 98.2 | 5.0 |
| `features/budget/BudgetPage.tsx` | 513 | 3 | — | — |
| `features/suggestions/SuggestionsPage.tsx` | 446 | — | 97.4 | 4.65 |
| `features/menu/MenuPage.tsx` | 340 | **5, `bug_magnet`** | 94.0 | 5.3 |
| `features/profile/ProfilePage.tsx` | — | **5, `bug_magnet`** | — | — |
| `pages/AdminPage.tsx` | 329 | — | 98.3 | 4.93 |

Три из шести — bug magnets. Это не совпадение: 20 хуков в одном компоненте
означают, что любое изменение одного сценария рискует зацепить остальные.

## Что внутри HomePage

`export function HomePage()` начинается на **строке 73** и идёт до конца файла
(506) — **~433 строки одним телом**. Внутри:

- 20+ вызовов хуков: `useActivePoll`, `usePollById`, `useMyVotes`, `useVote`,
  `useWithdrawVote`, `useCompletePoll`, `useCancelPoll`, `useLastCompletedPoll`,
  `usePollResults`, `useMenuItems`, `useMyGroups`, `useDebts`, `useCredits`,
  `useMarkPaid`, `useActiveStoreRuns`, `useCreateStoreRun`, `useSSE`,
  `useRecurringSchedule` + 4 мутации recurring, `useAuth`, `useToast`,
  `useAppStore`, `useQueryClient`, `useNavigate`;
- четыре независимых сценария: голосование, deep link, разовые/recurring опросы,
  создание закупки;
- «барьер первого экрана» — синхронизация готовности ~8 запросов, чтобы экран
  не собирался четырьмя толчками (в коде подробнейший комментарий с историей
  трёх неудачных редакций).

Важно: **комментарии в этом файле очень хорошие** — они объясняют, почему
барьер смотрит на `isSuccess||isError`, а не на `data`, и почему `?? []` вынесен
из тела. Это знание нельзя потерять при рефакторинге. Оно и есть главный риск
задачи.

## Что делать (минимально достаточное)

Не «разбить на 10 компонентов». Извлечь **связки хуков по сценариям** в
локальные хуки — тогда тело компонента станет композицией, а знание останется
рядом с кодом, которое оно объясняет.

Предлагаемая нарезка в `features/home/hooks/`:

| Новый хук | Что забирает |
|---|---|
| `useHomePoll()` | активный опрос + deep link + голоса + голосование/отзыв + complete/cancel + SSE |
| `useHomeRecurring()` | расписание + 4 мутации recurring + `durationKeyOf`/`DURATION_TO_MINUTES` |
| `useHomeBudget()` | debts, credits, markPaid, `budgetRow` |
| `useHomeStoreRun()` | активные закупки + создание |
| `useFirstScreenBarrier(queries)` | барьер готовности — **вместе с комментарием целиком** |

`features/home/lib/selectors.ts` уже существует и держит `budgetRow`,
`pollEndsAt`, `resolveTargetGroup` — то есть приём «вынести в lib» в этой фиче
уже применён. Продолжайте его, не вводите второй способ.

`BudgetPage.tsx` и `SuggestionsPage.tsx` — та же операция, но у них нет метки
bug magnet, поэтому они идут после HomePage.

## TDD-порядок

1. Прогнать существующие тесты HomePage, зафиксировать зелёный список.
2. **Дописать тесты на непокрытые сценарии до извлечения** — особенно на
   барьер первого экрана (он невидим в обычном тесте: проверяется, что до
   готовности всех запросов не отрендерены ни приветствие, ни талон).
3. Извлекать по одному хуку за коммит. Тесты не меняются — это доказательство
   сохранения поведения.
4. Тесты на извлечённые хуки — отдельно, через `renderHook` (см. задачу 11).

## Подводные камни

- **Барьер сломать проще всего.** Он зависит от того, что все запросы созданы
  **в одном рендере** и их статусы читаются вместе. Если извлечь запросы в
  четыре хука, порядок их вызова сохранится (правила хуков), но легко потерять
  сам барьер — он должен получать объекты запросов, а не распакованные данные.
  В комментарии прямо сказано: «"приехало" — это `isSuccess` или `isError`,
  а не наличие `data`». Извлечённый хук должен принимать `query`-объекты.
- **`?? []` в теле создаёт новый массив каждый рендер.** Комментарий это
  фиксирует: список берётся деструктуризацией с умолчанием
  (`const { data: allMenu = [] } = menuQuery`), а не `menuQuery.data ?? []`.
  При переносе в хук легко вернуть `?? []` в return — и `useMemo` ниже начнёт
  пересчитываться каждый рендер. Проверяйте референсную стабильность
  (тест: два рендера подряд без изменения данных → тот же референс).
- **`useSSE` привязан к `activePoll?.id`.** Если `useHomePoll` вернёт id
  позже, чем сейчас, подписка SSE подключится с задержкой и живые обновления
  «иногда не приходят» — классическая невоспроизводимая жалоба. Тест: id
  доступен в том же рендере, что и опрос.
- **Порядок инвалидации кэша относительно SSE.** Мутации (`useVote`,
  `useWithdrawVote`, `useCompletePoll`, `useCancelPoll`, `useMarkPaid`,
  `useCreateStoreRun` и мутации recurring) держат `onSuccess`, замкнутый на
  `qc` (`useQueryClient`). При переносе в `useHomePoll`/`useHomeBudget` меняется
  **порядок инвалидации относительно SSE-слушателя** из `useSSE`: если
  инвалидация теперь происходит до/после прихода события, экран начнёт
  «иногда не обновляться после голосования» — жалоба, которая не
  воспроизводится и не ловится тестом на один рендер. Перед переносом
  зафиксируйте тестом: голос → в кэше новые данные, независимо от того, пришло
  ли SSE-событие. Барьер и инвалидация — два разных механизма, не путайте их.
- **Deep link имеет приоритет над активным опросом** (`activePoll = deepLinkPollId ? deepLinkPoll ?? null : fallbackActivePoll`).
  Это неочевидное правило, легко теряемое. Тест на оба пути — до правки.
- **FAB удалён осознанно** (см. заголовочный комментарий: «Запустить
  голосование» — CTA талона, «Новая закупка» — кнопка секции «Сейчас»).
  Не возвращайте FAB «для удобства» — это решение продукта.
- **Правила визуального слоя из `CLAUDE.md` действуют:** не `transition-all`,
  анимировать `transform`/`opacity`, состояния `hover`/`focus-visible`/`active`,
  касание ≥44×44, проверять светлую/тёмную темы и ширину 390px. Рефакторинг
  не должен ничего из этого потерять.

## Критерии готовности

- [ ] Тело `HomePage()` ≤ 150 строк (сокращение с 433 — почти втрое; более
      агрессивная цель означала бы дробление ради цифры).
- [ ] Файл ≤ 260 строк.
- [ ] Все комментарии-обоснования переехали вместе с кодом, который объясняют.
- [ ] Есть тест на барьер первого экрана и на приоритет deep link.
- [ ] Health-скор HomePage > 6.0; метка `bug_magnet` перестала расти
      (новых багфиксов после рефакторинга нет).
- [ ] Скриншот-сравнение до/после: визуальных изменений нет (по `CLAUDE.md` —
      не менее двух проходов исправлений).

## Проверка

```powershell
npm --prefix frontend-new run type-check
npm --prefix frontend-new run lint
npm --prefix frontend-new test
npm --prefix frontend-new run test:e2e:smoke
npm --prefix frontend-new run build
```
